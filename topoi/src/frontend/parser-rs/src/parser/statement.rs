use crate::parser::Parser;
use crate::lexer::Token;

impl<'ast> Parser<'ast> {
    // pub fn value_definition(&mut self) -> Option<String> {
    //     let start = self.mark_start_span();

    //     let name = match self.current_token.as_ref().unwrap() {
    //         Token::Identifier => self.lexer.slice();
    //         _ => None
    //     };


    //     "a"
    // }
}